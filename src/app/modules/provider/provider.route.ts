import { Router } from "express";

export const providerRouter = Router();

/**
GET /provider/meals
POST /provider/meals
PATCH /provider/meals/:id
DELETE /provider/meals/:id
 */

// providerRouter.post("/meals",)